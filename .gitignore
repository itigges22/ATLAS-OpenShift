# ===========================================
# ATLAS .gitignore
# ===========================================

# ----- User Configuration -----
atlas.conf
.jwt_secret
*.local.conf
*.local
# Compose dev-mode override (local copy of docker-compose.override.yml.example)
docker-compose.override.yml
deploy/openshift/openshift.env

# ----- Development Task Files -----
TASK*.md
FIX_*.md
IMPLEMENT_*.md
ADD_*.md
CREATE_*.md
AUDIT*.md
*AUDIT*.md
FINAL_CLEANUP.md
# Local-only issue tracker (mirrored to GitHub Issues for public tracking).
ISSUES.md
DELETE_BEFORE_PUSH/
/plans/
docs/plans/

# ----- AI/Assistant Related (should not exist) -----
*claude*
*Claude*
.claude/
claude-*/
CLAUDE.md

# ----- Python -----
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.pytest_cache/
.coverage
htmlcov/
.tox/
.nox/
.hypothesis/
*.egg-info/
dist/
build/
eggs/
*.egg
.mypy_cache/
.ruff_cache/

# ----- Node.js -----
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json

# ----- IDE -----
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# Lens SQLite state from host/dev runs (containers keep it on the
# lens-state volume; a bare `uvicorn main:app` drops it in the cwd)
geometric_state.db
geometric_state.db-wal
geometric_state.db-shm

# ----- Logs -----
*.log
logs/
*_test_run.txt
*_output.txt

# ----- Models (too large for git) -----
*.gguf
*.bin
*.safetensors
*.pt
*.pth

# ----- Data directories -----
data/
training_data/
projects/

# ----- Secrets -----
*.pem
*.key
*.crt
.env
.env.*
!.env.example
secrets/
*secret*

# ----- Temporary files -----
tmp/
temp/
*.tmp
*.bak
*.backup

# ----- Test artifacts -----
.coverage
coverage.xml
junit.xml
test-results/
htmlcov/

# ----- Build artifacts -----
*.tar
*.tar.gz
*.zip

# Binaries
bin/
*.exe
*-darwin-*
*-linux-*

# ----- Development artifacts -----
validation_results.json
validate_geometric_lens.py
v2_verification_report.md

# ----- V1 vestiges (embedding-service replaced; V2.5 uses nomic-embed sidecar) -----
embedding-service/

# ----- V1 manifest templates (Qdrant and embedding-service removed in V2) -----
templates/qdrant-deployment.yaml.tmpl
templates/embedding-deployment.yaml.tmpl

# ----- K3s render output -----
# manifests/ is generated from templates/*.yaml.tmpl by
# scripts/generate-manifests.sh (or install.sh's process_templates step) at
# install time. Source of truth is templates/, not manifests/.
manifests/

# ----- Local data (not for git — user provides their own models) -----
/models/

# ----- Benchmark -----
benchmark/datasets/.cache/
benchmark/results/
benchmark/result/
v2_5_results/
V2_5_RALPH_LOOP_PROMPT.md

# ----- V2.5 results (development history, not final published result) -----
v2_5_1_results/
v2_5_1/

# ----- V3 ablation results: COMMITTED (raw evidence behind 74.6% claim) -----
# v3_ablation_results/ is NOT gitignored — it contains per-task pass/fail data
# for all ablation conditions (A-F) needed to verify published numbers.

# ----- Telemetry -----
*.jsonl

# ATLAS workspace (sandbox staging, backups)
.atlas/

# /demo per-run sandbox dirs — kept on disk so you can review what each
# side produced after recording; never checked in.
.demo-raw-*/
.demo-v3-*/

# ----- Go build artifacts -----
proxy/atlas-proxy
proxy/atlas-proxy-v2
proxy/cmd/
tui/atlas-tui

# ----- Geometric Lens large files (on HuggingFace, not in git) -----
geometric-lens/geometric_lens/models/training_embeddings_*.json
geometric-lens/geometric_lens/models/*_backup.pt
geometric-lens/geometric_lens/models/cost_field_phase0.pt
# Keep: cost_field.pt, gx_xgboost.pkl, gx_weights.json,
# gx_thresholds.json, model_identity.json, stats files
# Keep: geometric-lens/data/sample/ (small in-repo sample). The broad
# `data/` rule above would swallow it, so re-include the path chain.
!geometric-lens/data/
geometric-lens/data/*
!geometric-lens/data/sample/

# ----- Runtime artifacts -----
*.pid
nohup.out
highscores.json

# ----- Geometric Lens analysis artifacts (one-off, not runtime) -----
geometric-lens/geometric_lens/gate_analysis.py
geometric-lens/geometric_lens/gate_embeddings.json
geometric-lens/geometric_lens/gate_report.json
_agenttest/

# Lens training corpus collected from agent use (local, per-machine)
lens_training/
.atlas-upgrade/
geometric-lens/geometric_lens/models/provenance.json
geometric-lens/geometric_lens/models/provenance.json.sig
geometric-lens/geometric_lens/models/.previous-bundle/
