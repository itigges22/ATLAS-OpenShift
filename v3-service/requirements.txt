# V3 pipeline service runtime dependencies. Pinned to the versions the
# current image resolves so builds are reproducible. Torch is CPU-only
# (all model inference stays in llama-server) and installed from
# PyTorch's CPU index by the Dockerfile before this file is resolved;
# the pin here documents the runtime contract and lets pip verify the
# preinstalled wheel satisfies it.
#
# tree-sitter grammars back /internal/ast_edit and the call-graph
# reasoning (#39); the language set matches the selector translator —
# extend here when adding a language.
torch==2.13.0
tree-sitter==0.26.0
tree-sitter-python==0.25.0
tree-sitter-html==0.23.2
tree-sitter-javascript==0.25.0
